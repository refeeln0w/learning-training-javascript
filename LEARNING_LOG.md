# บันทึกการเรียนรู้ JavaScript & React

- **เป้าหมาย:** เรียนรู้ JavaScript ES6+ เพื่อปูพื้นฐานสู่ React
- **สถานะล่าสุด:** สำเร็จแบบฝึกหัดระดับที่ 1 (Array Methods พื้นฐาน)
- **ไฟล์งาน:** `01-javascript/03-array-method.js`

---

## 📝 สรุปความรู้: ระดับ 1 - Array Methods พื้นฐาน

| Method | การทำงาน | ค่าที่ได้กลับมา | ตัวอย่างการใช้งานจริงใน React |
| :--- | :--- | :--- | :--- |
| **`.filter()`** | กรองเฉพาะสมาชิกที่เงื่อนไขเป็น `true` | **Array ใหม่** (ความยาวลดลงหรือเท่าเดิม) | กรองข้อมูลแสดงผล, ลบรายการสินค้า/Todo |
| **`.map()`** | แปลงสมาชิกแต่ละตัวตามที่กำหนด | **Array ใหม่** (ความยาวเท่าเดิมเสมอ) | วนลูปเรนเดอร์ JSX / Component |
| **`.find()`** | ค้นหาสมาชิกตัวแรกที่ตรงเงื่อนไข | **Object ชิ้นเดียว** (หรือ `undefined` ถ้าไม่เจอ) | หาข้อมูลตาม `id` เพื่อเปิด Modal / หน้า Detail |
| **Chaining** | นำ method มาร้อยต่อกัน เช่น `.filter().map()` | **Array ใหม่** ตามขั้นตอนสุดท้าย | กรองสินค้าพร้อมส่ง แล้วดึงเฉพาะชื่อมาแสดง |

---

## 💡 สิ่งที่ได้เรียนรู้และข้อควรระวัง (Key Takeaways)

1. **เครื่องหมาย `=` vs `===`:**
   - `=` เป็นการกำหนดค่า (Assignment) ส่งผลให้เงื่อนไขเป็น truthy เสมอ
   - `===` เป็นการเปรียบเทียบค่าตรงกัน (Strict Equality)
2. **Callback Parameter & Variable Shadowing:**
   - ในแต่ละรอบของการวนลูป ข้อมูลที่ส่งเข้ามาคือ Object ทั้งตัว ต้องเข้าถึง property ผ่าน `product.propertyName`
   - แนะนำตั้งชื่อ Array รวมเป็นพหูพจน์ (`products`) และ parameter ย่อยเป็นเอกพจน์ (`product`) เพื่อไม่ให้ทับซ้อนกัน
3. **การเขียนเงื่อนไขแบบสั้น:**
   - สำหรับค่า Boolean: ใช้ `product.inStock` ได้ทันทีโดยไม่ต้อง `=== true`
   - ใช้ Logical AND (`&&`): ช่วยรวมหลายเงื่อนไขให้อยู่ใน filter เดียวได้อย่างกระชับ

---

## 🎯 ก้าวต่อไป (Next Step)
- **ระดับ 2: Immutability Pattern (หัวใจของ React State)**
  - การเพิ่มข้อมูลใหม่โดยไม่ใช้ `.push()` (ใช้ Spread Operator `...`)
  - การลบข้อมูลตาม `id` โดยไม่ใช้ `.splice()` (ใช้ `.filter()`)
  - การแก้ไขข้อมูลตาม `id` (ใช้ `.map()` + Spread Operator)